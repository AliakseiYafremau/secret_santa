version: "3.9"

services:
  secret-santa-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    command: ["python", "-c", "from secret_santa.main import run; run()"]
